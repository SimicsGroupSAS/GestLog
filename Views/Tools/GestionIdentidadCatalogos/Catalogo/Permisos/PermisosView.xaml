<UserControl x:Class="GestLog.Views.Tools.GestionIdentidadCatalogos.Catalogo.Permisos.PermisosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel>
            <TextBlock Text="Gestión de Permisos" FontWeight="Bold" FontSize="18" Margin="0,0,0,10"/>
            <Button Content="Nuevo Permiso" Command="{Binding NuevoPermisoCommand}" Margin="0,0,0,10"/>
            <ListBox ItemsSource="{Binding Permisos}" SelectedItem="{Binding PermisoSeleccionado}" DisplayMemberPath="Nombre" Height="200"/>
            <Button Content="Editar" Command="{Binding EditarPermisoCommand}" Margin="0,10,0,0"/>
            <Button Content="Eliminar" Command="{Binding EliminarPermisoCommand}" Margin="0,5,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>
