<UserControl x:Class="Views.Tools.GestionIdentidadCatalogos.Catalogo.Roles.RolFormView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="400">
    <Grid>
        <StackPanel>
            <TextBlock Text="Formulario de Rol" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>
            <TextBox Text="{Binding Rol.Nombre, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,5"/>
            <TextBox Text="{Binding Rol.Descripcion, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,5"/>
            <Button Content="Guardar" Command="{Binding GuardarRolCommand}" Margin="0,10,0,0"/>
            <Button Content="Cancelar" Command="{Binding CancelarCommand}" Margin="0,5,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>
