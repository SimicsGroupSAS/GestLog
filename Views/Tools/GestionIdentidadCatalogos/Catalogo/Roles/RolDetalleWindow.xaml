<Window x:Class="GestLog.Views.Tools.GestionIdentidadCatalogos.Catalogo.Roles.RolDetalleWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Detalles del Rol" Height="520" Width="520" WindowStartupLocation="CenterOwner" ResizeMode="NoResize" WindowStyle="SingleBorderWindow">    <Border Background="White" CornerRadius="12" Padding="32" Margin="0" Effect="{DynamicResource DropShadowEffect}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <TextBlock Grid.Row="0" Text="Detalles del Rol" FontSize="22" FontWeight="Bold" Foreground="#504F4E" Margin="0,0,0,18"/>
            
            <StackPanel Grid.Row="1">
                <TextBlock Text="Nombre" FontWeight="SemiBold" FontSize="14" Foreground="#888"/>
                <TextBlock Text="{Binding Nombre}" FontSize="16" FontWeight="Bold" Margin="0,0,0,8"/>
            </StackPanel>
            
            <StackPanel Grid.Row="2">
                <TextBlock Text="Descripción" FontWeight="SemiBold" FontSize="14" Foreground="#888"/>
                <TextBlock Text="{Binding Descripcion}" FontSize="15" TextWrapping="Wrap" Margin="0,0,0,16"/>
            </StackPanel>
            
            <TextBlock Grid.Row="3" Text="Permisos otorgados" FontWeight="SemiBold" FontSize="15" Foreground="#504F4E" Margin="0,0,0,8"/>
            
            <ScrollViewer Grid.Row="4" VerticalScrollBarVisibility="Auto" Margin="0,0,0,16">
                <ItemsControl ItemsSource="{Binding PermisosPorModulo}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="0,0,0,10">
                                <TextBlock Text="{Binding Modulo}" FontWeight="Bold" FontSize="14" Foreground="#1976D2"/>
                                <ItemsControl ItemsSource="{Binding Permisos}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Nombre, StringFormat='• {0}'}" FontSize="13" Margin="12,0,0,0"/>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
            
            <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cerrar" IsCancel="True" Background="#706F6F" Foreground="White" Padding="12,8" MinWidth="80" Height="36"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
