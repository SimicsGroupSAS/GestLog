<Window x:Class="GestLog.Views.Tools.GestionIdentidadCatalogos.Usuario.RestablecerContrasenaWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Restablecer Contraseña" SizeToContent="Height" Width="450"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="#FAFAFA">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Título -->
        <TextBlock Grid.Row="0" Text="Restablecer Contraseña" FontSize="18" FontWeight="SemiBold" 
                   Foreground="#118938" Margin="0,0,0,20" HorizontalAlignment="Center"/>

        <!-- Usuario -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock Text="Usuario:" Width="120" VerticalAlignment="Center" FontWeight="SemiBold"/>
            <TextBlock x:Name="txtUsuario" Text="{Binding NombreUsuario}" FontWeight="Bold" Foreground="#2B8E3F"/>
        </StackPanel>

        <!-- Nueva contraseña -->
        <StackPanel Grid.Row="2" Margin="0,0,0,15">
            <TextBlock Text="Nueva contraseña:" Margin="0,0,0,5" FontWeight="SemiBold"/>
            <PasswordBox x:Name="passwordBox" Height="32" Background="#FFFFFF" BorderBrush="#BDBDBD" 
                         BorderThickness="1" Padding="8" FontSize="14"/>
            <TextBlock Text="Mínimo 6 caracteres" FontSize="11" Foreground="#757575" Margin="0,2,0,0"/>
        </StackPanel>

        <!-- Confirmar contraseña -->
        <StackPanel Grid.Row="3" Margin="0,0,0,15">
            <TextBlock Text="Confirmar contraseña:" Margin="0,0,0,5" FontWeight="SemiBold"/>
            <PasswordBox x:Name="confirmPasswordBox" Height="32" Background="#FFFFFF" BorderBrush="#BDBDBD" 
                         BorderThickness="1" Padding="8" FontSize="14"/>
        </StackPanel>

        <!-- Mensaje de error -->
        <TextBlock x:Name="txtError" Grid.Row="4" Text="" Foreground="#C0392B" FontWeight="SemiBold"
                   Visibility="Collapsed" Margin="0,0,0,10"/>

        <!-- Información adicional -->
        <Border Grid.Row="5" Background="#E8F5E8" BorderBrush="#C8E6C9" BorderThickness="1" 
                CornerRadius="4" Padding="10" Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="💡 Recomendaciones:" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <TextBlock Text="• Use al menos 8 caracteres" FontSize="12"/>
                <TextBlock Text="• Combine letras, números y símbolos" FontSize="12"/>
                <TextBlock Text="• Evite información personal" FontSize="12"/>
            </StackPanel>
        </Border>        <!-- Botones -->
        <StackPanel Grid.Row="7" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancelar" Width="100" Height="32" Margin="0,0,10,0"
                    Background="#757575" Foreground="White" BorderThickness="0"
                    Click="CancelarButton_Click">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="4">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#616161"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
            <Button Content="Restablecer" Width="100" Height="32"
                    Background="#FF9800" Foreground="White" BorderThickness="0" FontWeight="Bold"
                    Click="RestablecerButton_Click">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="4">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F57C00"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>
    </Grid>
</Window>
