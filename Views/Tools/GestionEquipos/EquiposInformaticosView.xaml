<UserControl x:Class="GestLog.Views.Tools.GestionEquipos.EquiposInformaticosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:conv="clr-namespace:GestLog.Converters"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="900">
    <UserControl.Resources>
        <conv:BooleanToOpacityConverter x:Key="BooleanToOpacityConverter"/>
    </UserControl.Resources>
    <Grid Background="#FAFAFA">
        <StackPanel Margin="24,0,24,64" VerticalAlignment="Top">
            <TextBlock Text="Listado de Equipos Informáticos" FontSize="18" FontWeight="SemiBold" Foreground="#118938" Margin="0,0,0,18"/>
            <DataGrid x:Name="dgEquipos" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" SelectionMode="Single" HeadersVisibility="Column" RowHeaderWidth="0"
                      ItemsSource="{Binding ListaEquiposInformaticos}"
                      Background="White" BorderBrush="#E5E5E5" BorderThickness="1"
                      ColumnHeaderHeight="38" RowHeight="34" FontSize="14"
                      HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Código" Binding="{Binding Codigo}" Width="120"/>
                    <DataGridTextColumn Header="Usuario Asignado" Binding="{Binding UsuarioAsignado}" Width="180"/>
                    <DataGridTextColumn Header="Nombre de Equipo" Binding="{Binding NombreEquipo}" Width="180"/>
                    <DataGridTextColumn Header="Sede" Binding="{Binding Sede}" Width="120"/>
                    <DataGridTemplateColumn Header="Detalles" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Ver Detalles" 
                                        Command="{Binding DataContext.VerDetallesCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                                        CommandParameter="{Binding}" 
                                        IsEnabled="{Binding DataContext.CanVerHistorial, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        Opacity="{Binding DataContext.CanVerHistorial, RelativeSource={RelativeSource AncestorType=DataGrid}, Converter={StaticResource BooleanToOpacityConverter}}"
                                        Style="{StaticResource PrimaryButtonStyle}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
        <!-- Botón flotante para agregar equipo -->
        <Button Width="56" Height="56" VerticalAlignment="Bottom" HorizontalAlignment="Right"
                Margin="0,0,32,32" Content="+" FontSize="28" FontWeight="Bold"
                Background="#118938" Foreground="White"
                BorderBrush="#E5E5E5" BorderThickness="2"
                Cursor="Hand"
                ToolTip="Agregar nuevo equipo"
                Command="{Binding AgregarEquipoCommand}"
                IsEnabled="{Binding CanCrearEquipo}"
                Opacity="{Binding CanCrearEquipo, Converter={StaticResource BooleanToOpacityConverter}}"
                Style="{StaticResource PrimaryButtonStyle}"/>
    </Grid>
</UserControl>
