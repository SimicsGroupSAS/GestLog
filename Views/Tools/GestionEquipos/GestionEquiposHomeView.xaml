<UserControl x:Class="GestLog.Views.Tools.GestionEquipos.GestionEquiposHomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:views="clr-namespace:GestLog.Views.Tools.GestionEquipos"
             xmlns:mantenimientos="clr-namespace:GestLog.Modules.GestionMantenimientos.Views"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1100">
    <Grid Background="White" Margin="10">
        <TabControl x:Name="tabEquipos" Background="#F5F5F5" BorderThickness="0" Padding="0">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="FontFamily" Value="Segoe UI"/>
                    <Setter Property="FontSize" Value="13"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="Padding" Value="18,10"/>
                    <Setter Property="Foreground" Value="#504F4E"/>
                    <Setter Property="Background" Value="White"/>
                    <Setter Property="BorderBrush" Value="#E5E5E5"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Border Name="Border" Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1,1,1,0"
                                        Margin="2,0" CornerRadius="6,6,0,0">
                                    <ContentPresenter x:Name="ContentSite" VerticalAlignment="Center"
                                                     HorizontalAlignment="Center" ContentSource="Header"
                                                     Margin="{TemplateBinding Padding}"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="#F5F5F5"/>
                                        <Setter TargetName="Border" Property="BorderBrush" Value="#E5E5E5"/>
                                        <Setter TargetName="Border" Property="BorderThickness" Value="1,1,1,0"/>
                                        <Setter Property="Foreground" Value="#504F4E"/>
                                        <Setter Property="Panel.ZIndex" Value="1"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.Resources>
            <TabItem Header="Equipos">
                <Border Background="#F5F5F5" Padding="0" CornerRadius="0 0 8 8">
                    <views:EquiposInformaticosView />
                </Border>
            </TabItem>
            <TabItem Header="Cronograma">
                <Border Background="#F5F5F5" Padding="0" CornerRadius="0 0 8 8">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <mantenimientos:CronogramaDiarioView x:Name="cronogramaView" DataContext="{Binding CronogramaVm}" />

                        <!-- Indicador de depuración: mostrar cuántos items están en Planificados dentro del DataContext del control cronograma -->
                        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="8">
                            <TextBlock Text="Cronograma DataContext planificados: " VerticalAlignment="Center" />
                            <TextBlock Text="{Binding DataContext.Planificados.Count, ElementName=cronogramaView}" FontWeight="Bold" VerticalAlignment="Center" Margin="4,0,0,0" />
                        </StackPanel>
                    </Grid>
                </Border>
            </TabItem>
            <TabItem Header="Historial">
                <Border Background="#F5F5F5" Padding="0" CornerRadius="0 0 8 8">
                    <TextBlock Text="Historial de cambios y auditoría" Margin="16"/>
                </Border>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
