<Window x:Class="GestLog.Views.Tools.GestionEquipos.DetallesEquipoInformaticoView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:conv="clr-namespace:GestLog.Converters"
        mc:Ignorable="d"
        Title="Detalles Equipo" Height="720" Width="1000" WindowStartupLocation="CenterOwner" Background="#FAFAFA">
    <Window.Resources>
        <conv:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <conv:BooleanToEmojiConverter x:Key="BooleanToEmojiConverter"/>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#504F4E"/>
        </Style>
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#2B8E3F"/>
        </Style>

        <!-- Style para TextBox en modo solo lectura: se ve como texto pero permite selección -->
        <Style TargetType="TextBox">
            <Setter Property="Foreground" Value="#222222"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="IsReadOnlyCaretVisible" Value="True"/>
            <Setter Property="Cursor" Value="IBeam"/>
            <Setter Property="Focusable" Value="True"/>
            <Setter Property="SelectionBrush" Value="#BEE6FD"/>
            <Setter Property="SelectionOpacity" Value="0.9"/>
            <Setter Property="CaretBrush" Value="#222222"/>
            <Setter Property="TextOptions.TextFormattingMode" Value="Ideal"/>
            <Setter Property="TextOptions.TextRenderingMode" Value="Auto"/>
        </Style>

        <!-- Plantilla detallada para un slot RAM (valores como TextBox seleccionables) -->
        <DataTemplate x:Key="RamItemTemplate">
            <Border Margin="4" Padding="8" Background="#FFFFFF" CornerRadius="8" BorderBrush="#DDD" BorderThickness="1">
                <StackPanel Orientation="Vertical">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="Slot: " Style="{StaticResource LabelStyle}"/>
                        <TextBox Text="{Binding NumeroSlot, Mode=OneWay}" IsReadOnly="True" Margin="8,0,16,0"/>
                        <TextBlock Text="Capacidad (GB): " Style="{StaticResource LabelStyle}"/>
                        <TextBox Text="{Binding CapacidadGB, Mode=OneWay}" IsReadOnly="True" Margin="8,0,16,0"/>
                        <TextBlock Text="Tipo: " Style="{StaticResource LabelStyle}"/>
                        <TextBox Text="{Binding TipoMemoria, Mode=OneWay}" IsReadOnly="True" Margin="8,0,0,0"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                        <TextBlock Text="Marca: " Style="{StaticResource LabelStyle}"/>
                        <TextBox Text="{Binding Marca, Mode=OneWay}" IsReadOnly="True" BorderThickness="0" Background="Transparent" Padding="0" Margin="8,0,16,0"/>
                        <TextBlock Text="Frecuencia: " Style="{StaticResource LabelStyle}"/>
                        <TextBox Text="{Binding Frecuencia, Mode=OneWay}" IsReadOnly="True" BorderThickness="0" Background="Transparent" Padding="0" Margin="8,0,16,0"/>
                        <TextBlock Text="Ocupado: " Style="{StaticResource LabelStyle}"/>
                        <TextBox Text="{Binding Ocupado, Converter={StaticResource BooleanToEmojiConverter}, Mode=OneWay}" IsReadOnly="True" BorderThickness="0" Background="Transparent" Padding="0" Margin="8,0,0,0"/>
                    </StackPanel>
                    <TextBlock Text="Observaciones:" Style="{StaticResource LabelStyle}" Margin="0,6,0,0"/>
                    <TextBox Text="{Binding Observaciones, Mode=OneWay}" TextWrapping="Wrap" IsReadOnly="True" AcceptsReturn="True"/>
                </StackPanel>
            </Border>
        </DataTemplate>

        <!-- Plantilla detallada para un disco (valores como TextBox seleccionables) -->
        <DataTemplate x:Key="DiscoItemTemplate">
            <Border Margin="4" Padding="8" Background="#FFFFFF" CornerRadius="8" BorderBrush="#DDD" BorderThickness="1">
                <StackPanel Orientation="Vertical">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="#" Style="{StaticResource LabelStyle}"/>
                        <TextBox Text="{Binding NumeroDisco, Mode=OneWay}" IsReadOnly="True" Margin="8,0,12,0"/>
                        <TextBlock Text="Tipo: " Style="{StaticResource LabelStyle}"/>
                        <TextBox Text="{Binding Tipo, Mode=OneWay}" IsReadOnly="True" Margin="8,0,12,0"/>
                        <TextBlock Text="Capacidad (GB): " Style="{StaticResource LabelStyle}"/>
                        <TextBox Text="{Binding CapacidadGB, Mode=OneWay}" IsReadOnly="True" Margin="8,0,0,0"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                        <TextBlock Text="Marca: " Style="{StaticResource LabelStyle}"/>
                        <TextBox Text="{Binding Marca, Mode=OneWay}" IsReadOnly="True" Margin="8,0,16,0"/>
                        <TextBlock Text="Modelo: " Style="{StaticResource LabelStyle}"/>
                        <TextBox Text="{Binding Modelo, Mode=OneWay}" IsReadOnly="True" Margin="8,0,0,0"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </DataTemplate>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Background="#FFFFFF" CornerRadius="8" Padding="16" BorderBrush="#DDD" BorderThickness="1" Grid.Row="0">
            <Grid VerticalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <!-- Izquierda: título y código -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Grid.Column="0">
                    <TextBlock Text="Detalles del equipo" FontSize="18" FontWeight="Bold" Foreground="#118938" VerticalAlignment="Center"/>
                    <TextBlock Text="  -  " FontSize="18" VerticalAlignment="Center"/>
                    <TextBlock Text="Código: " Style="{StaticResource LabelStyle}" VerticalAlignment="Center"/>
                    <TextBox Text="{Binding Codigo, Mode=OneWay}" IsReadOnly="True" BorderThickness="0" Background="Transparent" Padding="0" VerticalAlignment="Center" Cursor="IBeam"/>
                </StackPanel>

                <!-- Derecha: botón Editar alineado a la derecha en la misma fila -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Right">
                    <Button Content="Editar" Width="90" Height="30" Margin="8,0,0,0" Click="BtnEditar_Click" Background="#2B8E3F" Foreground="White" FontWeight="SemiBold" />
                </StackPanel>
            </Grid>
        </Border>

        <!-- Metadatos y resumen en fila separada -->
        <Border Background="#FFFFFF" CornerRadius="8" Padding="12" BorderBrush="#DDD" BorderThickness="1" Grid.Row="1" Margin="0,12,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch" VerticalAlignment="Center">
                <StackPanel Orientation="Vertical" Margin="0,0,24,0">
                    <TextBlock Text="Fecha creación" Style="{StaticResource LabelStyle}"/>
                    <TextBox Text="{Binding FechaCreacion, StringFormat=d, Mode=OneWay}" IsReadOnly="True"/>
                </StackPanel>
                <StackPanel Orientation="Vertical" Margin="0,0,24,0">
                    <TextBlock Text="Fecha modificación" Style="{StaticResource LabelStyle}"/>
                    <TextBox Text="{Binding FechaModificacion, StringFormat=d, Mode=OneWay}" IsReadOnly="True"/>
                </StackPanel>
                <StackPanel Orientation="Vertical" Margin="0,0,24,0">
                    <TextBlock Text="Estado" Style="{StaticResource LabelStyle}"/>
                    <TextBox Text="{Binding Estado, Mode=OneWay}" IsReadOnly="True"/>
                </StackPanel>
                <StackPanel Orientation="Vertical" Margin="0,0,24,0">
                    <TextBlock Text="Sede" Style="{StaticResource LabelStyle}"/>
                    <TextBox Text="{Binding Sede, Mode=OneWay}" IsReadOnly="True"/>
                </StackPanel>
                <StackPanel Orientation="Vertical">
                    <TextBlock Text="Usuario asignado" Style="{StaticResource LabelStyle}"/>
                    <TextBox Text="{Binding UsuarioAsignado, Mode=OneWay}" IsReadOnly="True"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <Border Background="#FFFFFF" CornerRadius="8" Padding="16" BorderBrush="#DDD" BorderThickness="1" Grid.Row="2" Margin="0,12,0,12">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Margin="0,0,12,0">
                            <TextBlock Text="Nombre del equipo" Style="{StaticResource LabelStyle}"/>
                            <TextBox Text="{Binding NombreEquipo, Mode=OneWay}" IsReadOnly="True" />

                            <TextBlock Text="Marca" Style="{StaticResource LabelStyle}" Margin="0,8,0,0"/>
                            <TextBox Text="{Binding Marca, Mode=OneWay}" IsReadOnly="True" />

                            <TextBlock Text="Modelo" Style="{StaticResource LabelStyle}" Margin="0,8,0,0"/>
                            <TextBox Text="{Binding Modelo, Mode=OneWay}" IsReadOnly="True" />

                            <TextBlock Text="Sistema operativo" Style="{StaticResource LabelStyle}" Margin="0,8,0,0"/>
                            <TextBox Text="{Binding SO, Mode=OneWay}" IsReadOnly="True" />

                            <TextBlock Text="Serial" Style="{StaticResource LabelStyle}" Margin="0,8,0,0"/>
                            <TextBox Text="{Binding SerialNumber, Mode=OneWay}" IsReadOnly="True" />

                            <TextBlock Text="Procesador" Style="{StaticResource LabelStyle}" Margin="0,8,0,0"/>
                            <TextBox Text="{Binding Procesador, Mode=OneWay}" IsReadOnly="True" />

                            <TextBlock Text="Código AnyDesk" Style="{StaticResource LabelStyle}" Margin="0,8,0,0"/>
                            <TextBox Text="{Binding CodigoAnydesk, Mode=OneWay}" IsReadOnly="True" />
                        </StackPanel>

                        <StackPanel Grid.Column="1" Margin="12,0,0,0">
                            <TextBlock Text="Costo" Style="{StaticResource LabelStyle}"/>
                            <TextBox Text="{Binding Costo, StringFormat=C, Mode=OneWay}" IsReadOnly="True" />

                            <TextBlock Text="Fecha de compra" Style="{StaticResource LabelStyle}" Margin="0,8,0,0"/>
                            <TextBox Text="{Binding FechaCompra, StringFormat=d, Mode=OneWay}" IsReadOnly="True" />

                            <TextBlock Text="Fecha baja" Style="{StaticResource LabelStyle}" Margin="0,8,0,0"/>
                            <TextBox Text="{Binding FechaBaja, StringFormat=d, Mode=OneWay}" IsReadOnly="True" />

                            <TextBlock Text="Observaciones" Style="{StaticResource LabelStyle}" Margin="0,8,0,0"/>
                            <TextBox Text="{Binding Observaciones, Mode=OneWay}" TextWrapping="Wrap" AcceptsReturn="True" IsReadOnly="True" />

                            <Separator Margin="0,8"/>

                            <TextBlock Text="Fechas internas" Style="{StaticResource LabelStyle}" Margin="0,8,0,0"/>
                            <TextBox Text="{Binding FechaCreacionFormatted, Mode=OneWay}" IsReadOnly="True" />
                            <TextBox Text="{Binding FechaModificacionFormatted, Mode=OneWay}" IsReadOnly="True" />
                        </StackPanel>
                    </Grid>

                    <Separator Margin="0,8"/>

                    <!-- RAM -->
                    <Expander Header="{Binding RamHeader}" IsExpanded="True" Background="#F7FFF7" Padding="6">
                        <StackPanel>
                            <TextBlock Text="Cantidad de slots: " Style="{StaticResource LabelStyle}" />
                            <TextBox Text="{Binding SlotsRam.Count, Mode=OneWay}" IsReadOnly="True" Margin="0,0,0,8"/>

                            <ItemsControl ItemsSource="{Binding SlotsRam}" ItemTemplate="{StaticResource RamItemTemplate}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Orientation="Vertical"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                            </ItemsControl>
                        </StackPanel>
                    </Expander>

                    <Separator Margin="0,8"/>

                    <!-- Discos -->
                    <Expander Header="{Binding DiscoHeader}" IsExpanded="True" Background="#F7FFF7" Padding="6">
                        <StackPanel>
                            <TextBlock Text="Cantidad de discos: " Style="{StaticResource LabelStyle}" />
                            <TextBox Text="{Binding Discos.Count, Mode=OneWay}" IsReadOnly="True" Margin="0,0,0,8"/>

                            <ItemsControl ItemsSource="{Binding Discos}" ItemTemplate="{StaticResource DiscoItemTemplate}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Orientation="Vertical"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                            </ItemsControl>
                        </StackPanel>
                    </Expander>

                </StackPanel>
            </ScrollViewer>
        </Border>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="3">
            <Button Content="Cerrar" Width="100" Height="34" Margin="0,0,8,0" Click="BtnCerrar_Click" Background="#C0392B" Foreground="White"/>
        </StackPanel>
    </Grid>
</Window>
