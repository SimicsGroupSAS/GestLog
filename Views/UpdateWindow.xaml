<Window x:Class="GestLog.Views.UpdateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Actualizando GestLog" Height="220" Width="420" WindowStartupLocation="CenterScreen" ResizeMode="NoResize" WindowStyle="None" Background="#FAFAFA">    <Border CornerRadius="8" Background="White" Padding="24" Effect="{DynamicResource DropShadowEffect}">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="Actualizando GestLog..." FontSize="20" Foreground="#118938" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,16"/>
            <TextBlock Text="Por favor espera mientras se descarga e instala la actualización." Foreground="#504F4E" FontSize="14" HorizontalAlignment="Center" Margin="0,0,0,16"/>
            <ProgressBar x:Name="ProgressBar" Height="28" Width="320" Minimum="0" Maximum="100" Value="0" Background="#FFFFFF" Foreground="#118938" Margin="0,0,0,16"/>
            <TextBlock x:Name="ProgressText" Text="0%" FontSize="14" Foreground="#2B8E3F" HorizontalAlignment="Center" Margin="0,0,0,0"/>
        </StackPanel>
    </Border>
</Window>
