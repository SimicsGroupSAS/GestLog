<Window x:Class="GestLog.Modules.GestionVehiculos.Views.Dialog.ConfirmDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Confirmar" SizeToContent="WidthAndHeight" WindowStartupLocation="CenterOwner" ResizeMode="NoResize" WindowStyle="ToolWindow">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="TxtMessage" TextWrapping="Wrap" FontSize="14" Foreground="#222" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button x:Name="BtnNo" Content="No" Width="80" Margin="0,0,8,0" IsCancel="True" />
            <Button x:Name="BtnYes" Content="Sí" Width="80" IsDefault="True" />
        </StackPanel>
    </Grid>
</Window>
