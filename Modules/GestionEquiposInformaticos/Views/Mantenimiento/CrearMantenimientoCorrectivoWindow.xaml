<Window x:Class="GestLog.Modules.GestionEquiposInformaticos.Views.Mantenimiento.CrearMantenimientoCorrectivoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:conv="clr-namespace:GestLog.Converters"
        mc:Ignorable="d"
        Title="Crear Mantenimiento Correctivo"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        SizeToContent="Manual"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        d:DesignHeight="520" d:DesignWidth="720">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Modules/GestionEquiposInformaticos/Views/Equipos/ModalWindowsStandard.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <conv:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
            <conv:InverseBooleanConverter x:Key="InverseBooleanConverter" />
            <conv:NullToVisibilityConverter x:Key="NullToVisibilityConverter" />

            <!-- Estilo: botones con esquinas redondeadas -->
            <Style x:Key="RoundedButton" TargetType="Button">
                <Setter Property="MinHeight" Value="36" />
                <Setter Property="Padding" Value="10,6" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Background" Value="{StaticResource PrimaryBrush}" />
                <Setter Property="Foreground" Value="White" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" 
                                    BorderBrush="{TemplateBinding BorderBrush}" 
                                    BorderThickness="{TemplateBinding BorderThickness}" 
                                    CornerRadius="8" SnapsToDevicePixels="True">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <!-- OVERLAY MODAL - Fondo oscuro semitransparente -->
    <Grid x:Name="RootGrid" Background="#80000000" MouseLeftButtonDown="Overlay_MouseLeftButtonDown"
          UseLayoutRounding="True" SnapsToDevicePixels="True">

        <!-- CARD CENTRADA -->
        <Border x:Name="Card" Width="750" MaxHeight="700"
                Background="{StaticResource SurfaceBrush}"
                CornerRadius="8" Padding="0"
                HorizontalAlignment="Center" VerticalAlignment="Center"
                BorderThickness="1" BorderBrush="{StaticResource BorderBrush}"
                Effect="{StaticResource WindowShadow}" MouseLeftButtonDown="Panel_MouseLeftButtonDown">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- HEADER - Barra superior con gradiente -->
                <Border Grid.Row="0" Background="{StaticResource PrimaryBrush}" CornerRadius="8,8,0,0" 
                        Padding="24,16" Effect="{StaticResource HeaderShadow}">
                    <DockPanel>
                        <StackPanel DockPanel.Dock="Left" Orientation="Horizontal" VerticalAlignment="Center">
                            <Border Background="White" CornerRadius="6" Padding="6" Margin="0,0,16,0" 
                                    Width="36" Height="36" Effect="{StaticResource SectionShadow}">
                                <TextBlock Text="🔧" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="Crear mantenimiento correctivo" Style="{StaticResource HeaderTextStyle}"/>
                                <TextBlock Text="Registrar una nueva reparación o incidencia" Style="{StaticResource SubHeaderTextStyle}" Margin="0,3,0,0"/>
                            </StackPanel>
                        </StackPanel>

                        <!-- Botón Cerrar removido intencionalmente -->
                    </DockPanel>
                </Border>

                <!-- CONTENIDO - Scrolleable -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="24,20" 
                              Background="{StaticResource LightGrayBrush}">
                    <StackPanel>
                        <!-- Formulario -->
                        <StackPanel Orientation="Vertical" VerticalAlignment="Top" Width="680">
                            <TextBlock Text="Entidad (Equipo / Periférico)" FontWeight="SemiBold" Margin="0,0,0,6"/>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                <StackPanel.Resources>
                                    <conv:StringEqualsConverter x:Key="StringEqualsConverter" />
                                </StackPanel.Resources>
                                <RadioButton Content="Equipo" GroupName="EntidadGroup" Margin="0,0,12,0"
                                             IsChecked="{Binding TipoEntidad, Converter={StaticResource StringEqualsConverter}, ConverterParameter=Equipo, Mode=TwoWay}"/>
                                <RadioButton Content="Periférico" GroupName="EntidadGroup"
                                             IsChecked="{Binding TipoEntidad, Converter={StaticResource StringEqualsConverter}, ConverterParameter=Periférico, Mode=TwoWay}"/>
                            </StackPanel>                            <TextBlock Text="Código Equipo / Periférico" FontWeight="SemiBold" Margin="0,12,0,6"/>
                            <ComboBox ItemsSource="{Binding CodigoSugerencias}" IsEditable="True" IsTextSearchEnabled="False"
                                      SelectedItem="{Binding SelectedCodigoSuggestion, Mode=TwoWay}"
                                      Text="{Binding CodigoEntidad, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                      Padding="8" BorderBrush="{StaticResource BorderBrush}" Width="360"
                                      MaxDropDownHeight="240" StaysOpenOnEdit="False">
                                <!-- Mostrar las sugerencias con formato: "Código(Code) - Nombre(Name)" -->
                                <ComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock TextTrimming="CharacterEllipsis">
                                            <TextBlock.Text>
                                                <MultiBinding StringFormat="{}{0} - {1}">
                                                    <Binding Path="Code" />
                                                    <Binding Path="Name" />
                                                </MultiBinding>
                                            </TextBlock.Text>
                                        </TextBlock>
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>
                            </ComboBox>

                            <TextBlock Text="Fecha falla" FontWeight="SemiBold" Margin="0,12,0,6"/>
                            <DatePicker SelectedDate="{Binding FechaFalla}" Width="160"/>

                            <TextBlock Text="Descripción de la falla" FontWeight="SemiBold" Margin="0,12,0,6"/>
                            <TextBox Text="{Binding DescripcionFalla, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" Height="100" TextWrapping="Wrap" Padding="8" BorderBrush="{StaticResource BorderBrush}" VerticalScrollBarVisibility="Auto"/>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>

                <!-- FOOTER - Botones de acción -->
                <Border Grid.Row="2" Background="#F5F5F5" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0" 
                        CornerRadius="0,0,8,8" Padding="24,16">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Style="{StaticResource RoundedButton}" Content="Cancelar" Width="120" Background="#EEEEEE" Foreground="{StaticResource TextPrimaryBrush}" BorderThickness="1" BorderBrush="{StaticResource BorderBrush}" Click="CancelarButton_Click"/>
                        <Button Style="{StaticResource RoundedButton}" Content="Crear" Width="120" Margin="12,0,0,0" Background="{StaticResource PrimaryBrush}" Foreground="White" BorderThickness="0" FontSize="13" Command="{Binding SaveCommand}" IsEnabled="{Binding CanSave}" Opacity="{Binding CanSave, Converter={StaticResource BooleanToOpacityConverter}}"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
    </Grid>
</Window>
