<Window x:Class="GestLog.Modules.GestionMantenimientos.Views.Cronograma.SemanaDetalle.SemanaDetalleDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:GestLog.Converters"
        xmlns:local="clr-namespace:GestLog.Modules.GestionMantenimientos.Converters"
        xmlns:behaviors="clr-namespace:GestLog.Behaviors"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        Title="Detalle de la Semana" 
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"        WindowStartupLocation="Manual"
        ShowInTaskbar="False"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        Loaded="Window_Loaded"
        KeyDown="Window_KeyDown">    <Window.Resources>
        <converters:BooleanToStatusTextConverter x:Key="BooleanToStatusTextConverter" TrueText="Realizado" FalseText="Pendiente" />
        <converters:EnumToDescriptionConverter x:Key="EnumToDescriptionConverter" />
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <converters:InverseBooleanConverter x:Key="InverseBooleanConverter" />
        <converters:StringToVisibilityConverter x:Key="StringToVisibilityConverter" />
        <converters:MultiBooleanToVisibilityConverter x:Key="MultiBooleanToVisibilityConverter" />
        
        <!-- Converter para mapear Sede a Color de Fondo -->
        <local:SedeToBackgroundColorConverter x:Key="SedeToBackgroundColorConverter" />
        <!-- Badge de Tipo de Mantenimiento -->
        <Style x:Key="BadgePreventivo" TargetType="Border">
            <Setter Property="Background" Value="#E3F2FD"/>
            <Setter Property="BorderBrush" Value="#2196F3"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="8,4"/>
        </Style>        <Style x:Key="BadgeCorrectivo" TargetType="Border">
            <Setter Property="Background" Value="#F3E5F5"/>
            <Setter Property="BorderBrush" Value="#7E57C2"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="8,4"/>
        </Style>        <!-- Estilos visuales -->
        <Style x:Key="EquipoCellStyle" TargetType="DataGridCell">
            <Setter Property="Padding" Value="12,0,8,0"/>
        </Style>        <Style x:Key="DataGridCellStyle" TargetType="DataGridCell">
            <Setter Property="Padding" Value="8,0,8,0"/>
        </Style>        <!-- GroupStyle para agrupar por Sede con color de fondo en filas -->
        <Style x:Key="SedeGroupStyle" TargetType="GroupItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GroupItem">
                        <StackPanel>
                            <!-- Encabezado del Grupo (Sede) -->
                            <Border Background="#118938" Padding="12,8" Margin="0,6,0,0" CornerRadius="4">
                                <TextBlock Text="{Binding Name, StringFormat='🏢 {0}'}" 
                                           FontSize="13" FontWeight="Bold" Foreground="White"
                                           VerticalAlignment="Center"/>
                            </Border>
                            <!-- Items del Grupo -->
                            <ItemsPresenter />
                        </StackPanel>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- RowStyle con color de fondo dinámico por sede -->
        <Style x:Key="SedeRowStyle" TargetType="DataGridRow">
            <Setter Property="Background" Value="{Binding Sede, Converter={StaticResource SedeToBackgroundColorConverter}}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="Padding" Value="0"/>
        </Style>
        
        <Style x:Key="CardBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="BorderBrush" Value="#DDD"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#504F4E" BlurRadius="8" ShadowDepth="2" Opacity="0.18"/>
                </Setter.Value>
            </Setter>
        </Style>        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#118938"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="8,2"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#2B8E3F" BlurRadius="6" ShadowDepth="1" Opacity="0.18"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2B8E3F"/>
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect Color="#118938" BlurRadius="10" ShadowDepth="2" Opacity="0.28"/>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>        <!-- Modern Toggle Switch Style para orden de sedes -->
        <Style x:Key="SedeOrderToggleSwitchStyle" TargetType="ToggleButton">
            <Setter Property="Width" Value="200"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="VerticalContentAlignment" Value="Stretch"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Grid Background="Transparent">
                            <Border x:Name="BackgroundBorder" 
                                    Background="#F0F0F0" 
                                    CornerRadius="22" 
                                    BorderBrush="#DDD" 
                                    BorderThickness="1"
                                    Effect="{x:Null}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <!-- Indicador deslizante animado -->
                                    <Border x:Name="SliderBackground" 
                                            Grid.Column="0"
                                            Background="#118938" 
                                            CornerRadius="20"
                                            Margin="2,2,1,2"/>
                                    
                                    <!-- Opción Taller (izquierda) -->
                                    <StackPanel Grid.Column="0" 
                                                Orientation="Horizontal" 
                                                VerticalAlignment="Center" 
                                                HorizontalAlignment="Center"
                                                Margin="4,0">
                                        <TextBlock Text="🏭" FontSize="14" Margin="0,0,4,0"/>
                                        <TextBlock Text="Taller" VerticalAlignment="Center"/>
                                    </StackPanel>
                                    
                                    <!-- Opción Bayunca (derecha) -->
                                    <StackPanel Grid.Column="1" 
                                                Orientation="Horizontal" 
                                                VerticalAlignment="Center" 
                                                HorizontalAlignment="Center"
                                                Margin="4,0">
                                        <TextBlock Text="🏢" FontSize="14" Margin="0,0,4,0"/>
                                        <TextBlock Text="Bayunca" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </Grid>                        <ControlTemplate.Triggers>
                            <!-- Estado: Taller primero (chequeado) -->
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="BackgroundBorder" Property="Background" Value="#F0F0F0"/>
                                <Setter TargetName="SliderBackground" Property="Grid.Column" Value="0"/>
                                <Setter TargetName="SliderBackground" Property="Background" Value="#118938"/>
                            </Trigger>
                            <!-- Estado: Bayunca primero (no chequeado) -->
                            <Trigger Property="IsChecked" Value="False">
                                <Setter TargetName="BackgroundBorder" Property="Background" Value="#F0F0F0"/>
                                <Setter TargetName="SliderBackground" Property="Grid.Column" Value="1"/>
                                <Setter TargetName="SliderBackground" Property="Background" Value="#2196F3"/>
                            </Trigger>
                            <!-- Hover -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="BackgroundBorder" Property="BorderBrush" Value="#AAA"/>
                                <Setter TargetName="BackgroundBorder" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#000000" BlurRadius="8" ShadowDepth="2" Opacity="0.15"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>    <Grid MouseLeftButtonDown="Overlay_MouseLeftButtonDown" Background="#80000000">
        <Border Style="{StaticResource CardBorderStyle}" Margin="20" HorizontalAlignment="Center" VerticalAlignment="Center" MouseLeftButtonDown="Panel_MouseLeftButtonDown" Width="Auto" MinWidth="800" MaxWidth="1400">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>                <!-- Header fijo -->
                <StackPanel Grid.Row="0">
                    <!-- Título y Controles en una fila compacta -->
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <!-- Título (izquierda) -->
                        <TextBlock Grid.Column="0" Text="{Binding Titulo}" FontSize="18" FontWeight="Bold" Foreground="#118938" VerticalAlignment="Center"/>
                        
                        <!-- Información de la Semana (centro-derecha) -->
                        <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,0,0">
                            <TextBlock Text="📅" FontSize="14" Margin="0,0,6,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Semana:" FontSize="12" Foreground="#504F4E" FontWeight="SemiBold" Margin="0,0,4,0" VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding RangoFechas}" FontSize="12" Foreground="#504F4E" VerticalAlignment="Center" Margin="0,0,20,0"/>
                        </StackPanel>
                    </Grid>
                      <!-- Control de Orden de Sedes -->
                    <Border Background="#FAFAFA" CornerRadius="8" BorderBrush="#E0E0E0" BorderThickness="1" Padding="12,8">
                        <StackPanel>
                            <TextBlock Text="🔄 Ordenar Sedes" FontSize="11" FontWeight="Bold" Foreground="#118938" Margin="0,0,0,2"/>
                            <TextBlock Text="Selecciona cuál sede deseas ver primero en la lista:" FontSize="10" Foreground="#706F6F" Margin="0,0,0,6"/>
                            <ToggleButton IsChecked="{Binding SedeOrdenTallerPrimero}" 
                                          Style="{StaticResource SedeOrderToggleSwitchStyle}"
                                          ToolTip="Alterna entre mostrar Taller primero o Bayunca primero"
                                          HorizontalAlignment="Left"/>
                        </StackPanel>
                    </Border>
                    
                    <Separator Margin="0,12,0,0"/>
                </StackPanel><!-- Contenido scrolleable (DataGrid) -->
                <ScrollViewer x:Name="ContentScrollViewer" Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" PreviewMouseWheel="ScrollViewer_PreviewMouseWheel">
                    <StackPanel>
                        <TextBlock Text="Estados de Mantenimiento" FontSize="15" FontWeight="SemiBold" Foreground="#2B8E3F" Margin="0,10,0,6"/>
                  <!-- DataGrid que se adapta dinámicamente al espacio disponible en la pantalla -->
                <DataGrid x:Name="MantenimientosDataGrid" ItemsSource="{Binding EstadosMantenimientosView}" AutoGenerateColumns="False" IsReadOnly="True" Background="#FAFAFA" HeadersVisibility="Column" GridLinesVisibility="None" 
                          CanUserResizeColumns="True" CanUserResizeRows="False" CanUserSortColumns="True" 
                          HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Disabled" 
                          MinWidth="750" Margin="0,0,0,6"
                          BorderBrush="#DDD" BorderThickness="1"
                          SnapsToDevicePixels="True" UseLayoutRounding="True"
                          RowStyle="{StaticResource SedeRowStyle}">
                    
                    <!-- GroupStyle para mostrar encabezados de sede -->
                    <DataGrid.GroupStyle>
                        <GroupStyle ContainerStyle="{StaticResource SedeGroupStyle}">
                            <GroupStyle.Panel>
                                <ItemsPanelTemplate>
                                    <StackPanel IsItemsHost="True" />
                                </ItemsPanelTemplate>
                            </GroupStyle.Panel>
                        </GroupStyle>
                    </DataGrid.GroupStyle><DataGrid.Columns>
                        <DataGridTextColumn Header="Equipo" Binding="{Binding NombreEquipo}" MinWidth="140" Width="Auto" CellStyle="{StaticResource EquipoCellStyle}"/>
                        <DataGridTextColumn Header="Código" Binding="{Binding CodigoEquipo}" MinWidth="90" Width="Auto" CellStyle="{StaticResource DataGridCellStyle}"/>
                        <DataGridTextColumn Header="Sede" Binding="{Binding Sede, Converter={StaticResource EnumToDescriptionConverter}}" MinWidth="100" Width="Auto" CellStyle="{StaticResource DataGridCellStyle}"/>
                        <DataGridTemplateColumn Header="Tipo" MinWidth="130" Width="Auto">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                                        <Border x:Name="BadgePreventivo" Style="{StaticResource BadgePreventivo}" Visibility="Collapsed">
                                            <TextBlock Text="🔧 Preventivo" FontSize="12" FontWeight="SemiBold" Foreground="#1976D2"/>
                                        </Border>                                        <Border x:Name="BadgeCorrectivo" Style="{StaticResource BadgeCorrectivo}" Visibility="Collapsed">
                                            <TextBlock Text="⚠️ Correctivo" FontSize="12" FontWeight="SemiBold" Foreground="#7E57C2"/>
                                        </Border>
                                    </StackPanel>
                                    <DataTemplate.Triggers>
                                        <DataTrigger Binding="{Binding Seguimiento.TipoMtno}" Value="Preventivo">
                                            <Setter TargetName="BadgePreventivo" Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Seguimiento.TipoMtno}" Value="Correctivo">
                                            <Setter TargetName="BadgeCorrectivo" Property="Visibility" Value="Visible"/>
                                        </DataTrigger>                                    </DataTemplate.Triggers>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>                        <DataGridTextColumn Header="Frecuencia" Binding="{Binding Frecuencia}" MinWidth="110" Width="Auto" CellStyle="{StaticResource DataGridCellStyle}"/>
                        <DataGridTemplateColumn Header="Estado" MinWidth="140" Width="Auto">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Estado, Converter={StaticResource EnumToDescriptionConverter}}" FontWeight="SemiBold" FontSize="13"/>
                                        <TextBlock Margin="6,0,0,0" FontSize="14">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Text" Value=""/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Estado}" Value="RealizadoEnTiempo">
                                                            <Setter Property="Text" Value="✅"/>
                                                            <Setter Property="Foreground" Value="#118938"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Estado}" Value="RealizadoFueraDeTiempo">
                                                            <Setter Property="Text" Value="⏱️"/>
                                                            <Setter Property="Foreground" Value="#2B8E3F"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Estado}" Value="Atrasado">
                                                            <Setter Property="Text" Value="⚠️"/>
                                                            <Setter Property="Foreground" Value="#C0392B"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Estado}" Value="NoRealizado">
                                                            <Setter Property="Text" Value="❌"/>
                                                            <Setter Property="Foreground" Value="#C0392B"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Estado}" Value="Pendiente">
                                                            <Setter Property="Text" Value="⏸️"/>
                                                            <Setter Property="Foreground" Value="#706F6F"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="Acción" MinWidth="110" Width="Auto" CellStyle="{StaticResource DataGridCellStyle}">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Registrar"
                                            Command="{Binding DataContext.RegistrarMantenimientoCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                            CommandParameter="{Binding}"
                                            Style="{StaticResource PrimaryButtonStyle}"
                                            Padding="6,2"
                                            FontSize="12"
                                            Visibility="{Binding PuedeRegistrar, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>                        </DataGridTemplateColumn>                    </DataGrid.Columns>
                </DataGrid>
                  <TextBlock Text="No hay estados de mantenimiento para esta semana."
                           Foreground="#C0392B" FontWeight="SemiBold" FontSize="12" 
                           HorizontalAlignment="Center" Margin="0,8,0,8"
                           Visibility="{Binding EstadosMantenimientos.Count, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=False}"/>
                <TextBlock Text="{Binding MensajeUsuario}" Foreground="Red" FontWeight="SemiBold" Margin="0,0,0,6"
                           Visibility="{Binding MensajeUsuario, Converter={StaticResource StringToVisibilityConverter}}"/>
                    </StackPanel>
                </ScrollViewer>
                
                <!-- Footer fijo (sticky) -->
                <StackPanel Grid.Row="2">
                    <Separator Margin="0,8,0,8"/>                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Cerrar" Width="100" IsCancel="True" Click="OnCerrar_Click" Style="{StaticResource PrimaryButtonStyle}" FontSize="16" FontFamily="Segoe UI" FontWeight="Bold" SnapsToDevicePixels="True" TextOptions.TextFormattingMode="Display"/>
                </StackPanel>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>



